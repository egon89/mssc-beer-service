# used to configure properties needed during the bootstrap phase,
# such as properties related to connecting to an external configuration server
# and are read before the main application.properties file

spring.cloud.discovery.enabled=true
spring.cloud.config.discovery.service-id=mssc-config-server

